pre-commit:
  parallel: true
  commands:
    lint-staged:
      glob: "*.{js,jsx,ts,tsx}"
      run: yarn lint {staged_files} --fix && yarn format {staged_files} && yarn tsc --noEmit
      stage_fixed: true
